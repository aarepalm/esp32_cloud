alert_email = "your-email@example.com"

# Shared secret between the ESP32 device and the presign Lambda.
# Device sends this as the x-api-key header on every presign request.
# Use a long random string, e.g.: openssl rand -hex 32
presign_api_key = "change-me-use-a-long-random-string"

# Cognito gallery login credentials
# These are the username and password used to log into the web gallery.
# Password must be at least 8 characters with uppercase, lowercase, digit and symbol.
cognito_username = "admin"
cognito_password = "ChangeMe123!"
